{# {{ app.request.get(_route) === 'app_home' ? 'active' : '' )}"  #}
<div class=" justify-content-center">
    <nav class="navbar navbar-expand-lg shadow p-3 mb-5 bg-white rounded">
        <a class="navbar-brand" href={{ path('app_home') }}><img class="logo img-fluid" src="assets/img/logo-coupé.png"
                                                                 alt="Logo"></a>
        <img class="en-tete img-fluid " src="assets/img/en-tete-coupé.png" alt="en-tete">
        <div class="collapse flex-row-reverse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item active">
                    <a class="nav-link btnAcc p-2 m-3 " href={{ path('app_home') }}>Accueil
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link btnAcc p-2 m-3 " href={{ path('app_carte') }}>
                        Carte
                    </a>
                </li>
                <div class="btn-group" role="group">
                    <button type="button" class="btnAcc  p-2 m-3 dropdown-toggle" data-bs-toggle="dropdown"
                            aria-expanded="false">
                        Mon compte
                    </button>
                    <ul class="dropdown-menu">
                        {% if app.user %}
                            <li><a class="dropdown-item " href={{ path('app_logout') }}>Se déconnecter</a></li>
                        {% else %}
                            <li><a class="dropdown-item" href={{ path('app_login') }}>Se connecter</a></li>
                        {% endif %}
                        <li><a class="dropdown-item" href="#">Modifier mon compte</a></li>
                    </ul>
                </div>
                <div class="btn-group" role="group">
                    <button type="button" class="btnAcc p-2 m-3 dropdown-toggle" data-bs-toggle="dropdown"
                            aria-expanded="false">
                        Administration
                    </button>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('app_gallerie') }}">Gallerie</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_horaire') }}">Horaire</a></li>
                    </ul>
                </div>
                </li>
            </ul>
        </div>
    </nav>
</div>


